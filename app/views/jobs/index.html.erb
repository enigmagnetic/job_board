<h3>Jobs</h3>

<% @jobs.each do |job| %>
  <div class="job">
    <strong>
      <%= link_to job.title, job_path(job.id) %>
    </strong>
  </div>
<% end %>

<p>
  <%= link_to "Add a Job", jobs_new_path %>
